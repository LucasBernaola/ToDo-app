// MyComponent.test.js

describe('Example test', () => {
    it('should pass without testing any component', () => {
      // Hacemos alguna operación o verificación que esperamos que pase sin problemas
      const result = 2 + 2;
      // Verificamos que el resultado sea el esperado
      expect(result).toBe(4);
    });
  });